# Activate with: --spring.profiles.active=postgres
# Defaults match the bundled docker-compose.yml (db/user/password = conductor).
# Override for other deployments via SPRING_DATASOURCE_URL / _USERNAME / _PASSWORD.
conductor.db.type=postgres
conductor.queue.type=postgres

spring.datasource.url=jdbc:postgresql://localhost:5432/conductor
spring.datasource.username=conductor
spring.datasource.password=conductor
spring.datasource.hikari.maximum-pool-size=8
spring.datasource.hikari.auto-commit=false

conductor.indexing.enabled=true
conductor.indexing.type=postgres
conductor.external-payload-storage.type=postgres
conductor.workflow-execution-lock.type=postgres
